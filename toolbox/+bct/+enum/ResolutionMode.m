classdef ResolutionMode
    %RESOLUTIONMODE  Enumeration for domain resolution selection.
    %
    %   Full       = use full available domain resolution
    %   Instrument = use effective/instrument resolution
    
    enumeration
        Full
        Instrument
    end
end
